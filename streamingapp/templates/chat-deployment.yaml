{{- $s := .Values.services.chat }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat
spec:
  replicas: {{ $s.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: chat
  template:
    metadata:
      labels:
        app: chat
    spec:
      containers:
        - name: chat
          image: "{{ $s.image }}:{{ $s.tag }}"
          imagePullPolicy: {{ $.Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ $s.port }}
          env:
            - name: PORT
              value: "{{ $s.port }}"
          envFrom:
            - configMapRef:
                name: streamingapp-config
            - secretRef:
                name: streamingapp-secret
          readinessProbe:
            tcpSocket:
              port: {{ $s.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: {{ $s.port }}
            initialDelaySeconds: 20
            periodSeconds: 10
